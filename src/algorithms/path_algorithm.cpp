#include "path_algorithm.h"

const size_t PathAlgorithm::PATH_START = static_cast<size_t>(-1);
const size_t PathAlgorithm::PATH_NOT_VISITED = static_cast<size_t>(-2);

PathAlgorithm::PathAlgorithm()
{

}
